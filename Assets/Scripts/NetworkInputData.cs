using UnityEngine;

public class NetworkInputData
{
    public Vector2 move; // x = horizontal, y = vertical
    public bool jump;
    public bool run;
    public bool dash;
    public bool crouch;
}
